const initialState = {
    model: 'Tutorial',
    booking: 'booking from store',
    client: 'client from store',
    hotelier: 'hotelier from store',
    login: false
}

export function initReducer(state = initialState) {
    return state
}
